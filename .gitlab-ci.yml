image: node:lts

pages:
  cache:
    paths:
      - node_modules/
  before_script:
    - npm install
    - npm run build
  script:
    - ls -la public/ || ls -la dist/ || ls -la  # Ver qué se genera
    - echo "🎵 Debug completo"
  artifacts:
    paths:
      - public/  # ← VOLVEMOS A public
  only:
    - main
 