image: node:lts

pages:
  cache:
    paths:
      - node_modules/
  before_script:
    - npm install
    - npm run build
  script:
    - echo "🎵 DagKlassical - Build completado"
  artifacts:
    paths:
      - dist/  # ← CORRECCIÓN CLAVE: dist en lugar de public
  only:
    - main