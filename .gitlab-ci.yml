image: node:lts

pages:
  cache:
    paths:
      - node_modules/
  before_script:
    - npm install
    - npm run build
  script:
    - echo "🎵 DagKlassical - Build completado"
    - ls -la public/  # ← Ver qué archivos hay
    - echo "🎵 Build completado"
  artifacts:
    paths:
      - public/  # ← VOLVEMOS A public
  only:
    - main
 