---
// Header Component - DAG Klassical (CSS CENTRALIZADO)
// Archivo: src/components/Header.astro
// Propósito: Header único con CSS centralizado y JavaScript embebido

// CSS Centralizado Importado
import '../styles/components/header.css';
---

<header class="header">
  <div class="container">
    <nav class="nav">
      <!-- Logo y Brand -->
      <a href="/dag-www/" class="nav-brand">
        <img src="/dag-www/logo-dag.svg" alt="DAG Klassical" class="nav-logo" />
        <span class="text-gradient">DAG Klassical</span>
      </a>
      
      <!-- Menú Principal (Desktop) -->
      <ul class="nav-menu" id="nav-menu">
        <!-- Inicio -->
        <li class="nav-item">
          <a href="/dag-www/" class="nav-link" data-active={Astro.url.pathname === '/dag-www/' ? 'true' : 'false'}>
            <svg class="nav-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
              <polyline points="9,22 9,12 15,12 15,22"/>
            </svg>
            <span class="nav-text">Inicio</span>
          </a>
        </li>
        
        <!-- Contacto -->
        <li class="nav-item">
          <a href="/dag-www/contacto/" class="nav-link" data-active={Astro.url.pathname === '/dag-www/contacto' ? 'true' : 'false'}>
            <svg class="nav-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
              <polyline points="22,6 12,13 2,6"/>
            </svg>
            <span class="nav-text">Contacto</span>
          </a>
        </li>
        
        <!-- Música con Landing + Dropdown -->
        <li class="nav-item dropdown">
          <a href="/dag-www/musica/" class="nav-link dropdown-toggle" data-active={Astro.url.pathname.startsWith('/dag-www/musica') ? 'true' : 'false'}>
            <span class="nav-text">Música</span>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <polyline points="6,9 12,15 18,9"></polyline>
            </svg>
          </a>
          <div class="dropdown-menu">
            <a href="/dag-www/musica/" class="dropdown-item landing-link">🎵 Ver Todo</a>
            <a href="/dag-www/musica/artistas/" class="dropdown-item">Artistas</a>
            <a href="/dag-www/musica/discografia/" class="dropdown-item">Discografía</a>
            <a href="/dag-www/musica/lanzamientos/" class="dropdown-item">Lanzamientos</a>
            <a href="/dag-www/musica/preventa/" class="dropdown-item">Preventa</a>
          </div>
        </li>
        
        <!-- Blockchain con Landing + Dropdown -->
        <li class="nav-item dropdown">
          <a href="/dag-www/blockchain/" class="nav-link dropdown-toggle" data-active={Astro.url.pathname.startsWith('/dag-www/blockchain') ? 'true' : 'false'}>
            <span class="nav-text">Blockchain</span>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <polyline points="6,9 12,15 18,9"></polyline>
            </svg>
          </a>
          <div class="dropdown-menu">
            <a href="/dag-www/blockchain/" class="dropdown-item landing-link">🔗 Ver Todo</a>
            <a href="/dag-www/blockchain/digital-id/" class="dropdown-item">Digital ID</a>
            <a href="/dag-www/blockchain/tienda-nft/" class="dropdown-item">Tienda NFT</a>
            <a href="/dag-www/blockchain/tickets/" class="dropdown-item">Tickets</a>
            <a href="/dag-www/blockchain/polygon-networks/" class="dropdown-item">Polygon Networks</a>
          </div>
        </li>
        
        <!-- Noticias -->
        <li class="nav-item">
          <a href="/dag-www/noticias/" class="nav-link" data-active={Astro.url.pathname.startsWith('/dag-www/noticias') ? 'true' : 'false'}>
            <span class="nav-text">Noticias</span>
          </a>
        </li>
        
        <!-- Nosotros con Landing + Dropdown -->
        <li class="nav-item dropdown">
          <a href="/dag-www/nosotros/" class="nav-link dropdown-toggle" data-active={Astro.url.pathname.startsWith('/dag-www/nosotros') ? 'true' : 'false'}>
            <span class="nav-text">Nosotros</span>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <polyline points="6,9 12,15 18,9"></polyline>
            </svg>
          </a>
          <div class="dropdown-menu">
            <a href="/dag-www/nosotros/" class="dropdown-item landing-link">👥 Ver Todo</a>
            <a href="/dag-www/nosotros/dag-klassical/" class="dropdown-item">DAG Klassical</a>
            <a href="/dag-www/nosotros/danilo-alvarez/" class="dropdown-item">Danilo Álvarez</a>
            <a href="/dag-www/nosotros/equipo/" class="dropdown-item">Equipo</a>
            <a href="/dag-www/nosotros/terminos/" class="dropdown-item">Términos</a>
          </div>
        </li>
      </ul>
      
      <!-- Acciones de navegación -->
      <div class="nav-mobile-actions">
        <!-- Botón de Búsqueda -->
        <button class="search-toggle" id="search-toggle" aria-expanded="false" aria-label="Abrir búsqueda">
          <svg class="search-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <circle cx="11" cy="11" r="8"/>
            <path d="21 21l-4.35-4.35"/>
          </svg>
          <svg class="search-close-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" style="display: none;">
            <line x1="18" y1="6" x2="6" y2="18"/>
            <line x1="6" y1="6" x2="18" y2="18"/>
          </svg>
        </button>

        <!-- Menú Hamburguesa -->
        <button class="nav-toggle" id="nav-toggle" aria-expanded="false" aria-label="Abrir menú">
          <svg class="hamburger-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <line x1="3" y1="6" x2="21" y2="6"/>
            <line x1="3" y1="12" x2="21" y2="12"/>
            <line x1="3" y1="18" x2="21" y2="18"/>
          </svg>
          <svg class="close-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" style="display: none;">
            <line x1="18" y1="6" x2="6" y2="18"/>
            <line x1="6" y1="6" x2="18" y2="18"/>
          </svg>
        </button>
      </div>
      
      <!-- Botón de Wallet (Solo Desktop) -->
      <div class="nav-actions">
        <a href="#" class="btn-wallet">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <path d="M21 12V7H5a2 2 0 0 1 0-4h14v4a2 2 0 0 0-4 0v-5a2 2 0 0 0-4 0v-5a2 2 0 0 0-4 0v-5a2 2 0 0 1 0-4"/>
          </svg>
          <span class="wallet-text">Wallet</span>
        </a>
      </div>

      <!-- Campo de Búsqueda -->
      <div class="search-container" id="search-container">
        <div class="search-input-wrapper">
          <svg class="search-input-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <circle cx="11" cy="11" r="8"/>
            <path d="21 21l-4.35-4.35"/>
          </svg>
          <input 
            type="text" 
            class="search-input" 
            placeholder="Buscar artistas, música, contenido..."
            id="search-input"
          />
        </div>
        <div class="search-results" id="search-results">
          <!-- Resultados de búsqueda se mostrarán aquí -->
        </div>
      </div>
    </nav>
  </div>
</header>


<script>
  // JavaScript para menú hamburguesa y búsqueda
  const navToggle = document.getElementById('nav-toggle');
  const navMenu = document.getElementById('nav-menu');
  const searchToggle = document.getElementById('search-toggle');
  const searchContainer = document.getElementById('search-container');
  const searchInput = document.getElementById('search-input');
  
  if (navToggle && navMenu) {
    navToggle.addEventListener('click', () => {
      const isExpanded = navToggle.getAttribute('aria-expanded') === 'true';
      
      navToggle.setAttribute('aria-expanded', !isExpanded);
      navMenu.classList.toggle('active');
      navToggle.classList.toggle('active');
      
      // Cerrar búsqueda si está abierta
      if (searchContainer.classList.contains('active')) {
        searchContainer.classList.remove('active');
        searchToggle.setAttribute('aria-expanded', 'false');
        searchToggle.classList.remove('active');
      }
      
      // Prevenir scroll cuando menú está abierto
      document.body.style.overflow = isExpanded ? '' : 'hidden';
    });
    
    // Cerrar menú al hacer clic en un enlace
    const navLinks = navMenu.querySelectorAll('.nav-link');
    navLinks.forEach(link => {
      link.addEventListener('click', () => {
        navToggle.setAttribute('aria-expanded', 'false');
        navMenu.classList.remove('active');
        navToggle.classList.remove('active');
        document.body.style.overflow = '';
      });
    });
  }

  // Funcionalidad de búsqueda
  if (searchToggle && searchContainer) {
    searchToggle.addEventListener('click', () => {
      const isExpanded = searchToggle.getAttribute('aria-expanded') === 'true';
      
      searchToggle.setAttribute('aria-expanded', !isExpanded);
      searchContainer.classList.toggle('active');
      searchToggle.classList.toggle('active');
      
      // Cerrar menú si está abierto
      if (navMenu && navMenu.classList.contains('active')) {
        navMenu.classList.remove('active');
        navToggle.classList.remove('active');
        document.body.style.overflow = '';
      }
      
      // Focus en input cuando se abre
      if (!isExpanded && searchInput) {
        setTimeout(() => searchInput.focus(), 100);
      }
    });
  }

  // Funcionalidad de dropdowns en móvil (tap to toggle)
  const dropdowns = document.querySelectorAll('.dropdown');
  dropdowns.forEach(dropdown => {
    const toggle = dropdown.querySelector('.dropdown-toggle');
    
    if (toggle) {
      toggle.addEventListener('click', (e) => {
        // Solo para móvil y cuando el menú móvil esté abierto
        if (window.innerWidth <= 768 && navMenu && navMenu.classList.contains('active')) {
          e.preventDefault();
          e.stopPropagation();
          
          // Cerrar otros dropdowns
          dropdowns.forEach(otherDropdown => {
            if (otherDropdown !== dropdown && otherDropdown.classList.contains('mobile-open')) {
              otherDropdown.classList.remove('mobile-open');
            }
          });
          
          // Toggle este dropdown
          dropdown.classList.toggle('mobile-open');
        }
      });
    }
  });

  // Cerrar dropdowns al hacer clic fuera
  document.addEventListener('click', (e) => {
    if (!e.target.closest('.dropdown')) {
      dropdowns.forEach(dropdown => {
        dropdown.classList.remove('mobile-open');
      });
    }
  });

  // Cerrar búsqueda al hacer clic fuera
  document.addEventListener('click', (e) => {
    if (!e.target.closest('.search-container') && !e.target.closest('.search-toggle')) {
      searchContainer.classList.remove('active');
      searchToggle.classList.remove('active');
      searchToggle.setAttribute('aria-expanded', 'false');
    }
  });

  // Funcionalidad de búsqueda básica (simulada)
  if (searchInput) {
    searchInput.addEventListener('input', (e) => {
      const query = e.target.value.trim();
      const resultsContainer = document.getElementById('search-results');
      
      if (query.length > 2) {
        // Aquí iría la lógica real de búsqueda
        resultsContainer.innerHTML = `
          <div class="search-result-item">
            <strong>Resultados para:</strong> "${query}"
          </div>
          <div class="search-result-item">Virginia Ramírez</div>
          <div class="search-result-item">Sinfonía Digital</div>
          <div class="search-result-item">NFT Musical</div>
        `;
        resultsContainer.style.display = 'block';
      } else {
        resultsContainer.style.display = 'none';
      }
    });
  }

  // Manejar escape para cerrar modales
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      // Cerrar menú hamburguesa
      if (navMenu && navMenu.classList.contains('active')) {
        navToggle.setAttribute('aria-expanded', 'false');
        navMenu.classList.remove('active');
        navToggle.classList.remove('active');
        document.body.style.overflow = '';
      }
      
      // Cerrar búsqueda
      if (searchContainer && searchContainer.classList.contains('active')) {
        searchContainer.classList.remove('active');
        searchToggle.classList.remove('active');
        searchToggle.setAttribute('aria-expanded', 'false');
      }
      
      // Cerrar dropdowns móviles
      dropdowns.forEach(dropdown => {
        dropdown.classList.remove('mobile-open');
      });
    }
  });
</script>